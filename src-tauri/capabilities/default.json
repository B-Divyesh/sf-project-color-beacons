{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Choose local project folders and configure editor files.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
