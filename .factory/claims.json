[
  {
    "id": "three-cues",
    "claim": "Each project repeats a color, name, and symbol.",
    "where": "landing hero, demo, README",
    "test": "npm test -- --grep @claim:three-cues",
    "sandbox": "fresh browser context at /demo with the three shipped sample projects"
  },
  {
    "id": "confirmation-before-write",
    "claim": "Editor settings appear only after the named project is confirmed.",
    "where": "landing preview, demo, README",
    "test": "npm test -- --grep @claim:confirmation-before-write",
    "sandbox": "fresh /demo state; select Atlas API, assert no output, confirm it, then inspect both editor files"
  },
  {
    "id": "demo-isolated",
    "claim": "The demo saves only under its separate demo storage namespace and sends no project data away.",
    "where": "landing action note, demo banner, privacy page",
    "test": "npm test -- --grep @claim:demo-isolated",
    "sandbox": "fresh browser context; exercise the demo, inspect storage keys, and intercept every request"
  },
  {
    "id": "demo-disposal",
    "claim": "Start for real discards the isolated sample workspace.",
    "where": "demo banner, privacy page, demo contract",
    "test": "npm test -- --grep @claim:demo-disposal",
    "sandbox": "fresh site and desktop-demo browser contexts; select Northwind Store, leave the demo, assert the demo key is gone, and reopen the site demo in its initial state"
  },
  {
    "id": "demo-reset",
    "claim": "Reset demo restores the completed Atlas API sample and all three sample projects.",
    "where": "README, demo banner, demo contract",
    "test": "npm test -- --grep @claim:demo-reset",
    "sandbox": "fresh /demo state; confirm Northwind Store, reset, then assert Atlas API is confirmed with its editor preview and exactly three projects"
  },
  {
    "id": "offline-reload",
    "claim": "The demo reloads offline after its first visit.",
    "where": "landing facts, README, demo contract",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; load /demo, wait for its service worker, go offline, and reload"
  },
  {
    "id": "free-project-limit",
    "claim": "Color, name, symbol, and confirmation are free for three projects; a valid license removes that limit.",
    "where": "landing facts, pricing, app license dialog, README, terms",
    "test": "npm test -- --grep @claim:free-project-limit",
    "sandbox": "browser version of the app preloaded with three local projects; exercise all four free capabilities, try a fourth project, then verify a fixture license"
  },
  {
    "id": "beacon-stability",
    "claim": "A saved project's color, name, and symbol remain unchanged when the app reopens.",
    "where": "site metadata, README, product behavior",
    "test": "npm test -- --grep @claim:beacon-stability",
    "sandbox": "fresh desktop-demo browser context; save Payments Worker, close its page, reopen the app with the same local storage, and compare all three cues"
  },
  {
    "id": "release-manifest",
    "claim": "The release workflow generates SHA256SUMS and latest.json for its desktop packages.",
    "where": "README",
    "test": "npm test -- --grep @claim:release-manifest",
    "sandbox": "temporary release directory with macOS, Windows, and Linux fixture packages; run the shipped manifest generator and inspect both outputs"
  },
  {
    "id": "release-signing",
    "claim": "A recorded GitHub provenance file verifies each package's repository, workflow, commit, and tag identity.",
    "where": "README, release workflow",
    "test": "npm test -- --grep @claim:release-signing",
    "sandbox": "recorded GitHub Sigstore bundle; cryptographically verify the workflow identity, then assert the fixture package, repository, workflow, commit, and tag"
  },
  {
    "id": "release-matrix",
    "claim": "The release workflow targets macOS Intel and Apple silicon, Windows, and Linux packages.",
    "where": "README, release workflow",
    "test": "npm test -- --grep @claim:release-matrix",
    "sandbox": "inspect the shipped GitHub Actions workflow for both macOS targets plus Windows, Linux, and the Tauri build action"
  },
  {
    "id": "platform-download",
    "claim": "The landing page selects a matching package only after its platform trust check and every release record pass.",
    "where": "landing download section, README",
    "test": "npm test -- --grep @claim:platform-download",
    "sandbox": "three fresh browser contexts with macOS, Windows, and Linux user agents plus complete and incomplete recorded GitHub release responses"
  },
  {
    "id": "platform-signatures",
    "claim": "Linux downloads require verified GitHub provenance; Windows requires Authenticode; macOS requires signing and notarization.",
    "where": "README, release workflow, download gate",
    "test": "npm test -- --grep @claim:platform-signatures",
    "sandbox": "complete recorded release; unsigned Windows and macOS records keep those downloads closed without blocking verified Linux"
  },
  {
    "id": "settings-preserved",
    "claim": "Existing unrelated editor JSON settings stay in place.",
    "where": "how it works, README",
    "test": "npm test -- --grep @claim:settings-preserved",
    "sandbox": "Rust core merge fixture with an existing editor font size and activity-bar color"
  },
  {
    "id": "editor-settings",
    "claim": "The app writes supported settings for VS Code, Cursor, and Zed.",
    "where": "how it works, README",
    "test": "npm test -- --grep @claim:editor-settings",
    "sandbox": "Rust core fixture writes both supported editor files and checks their beacon settings"
  },
  {
    "id": "project-data-local",
    "claim": "Project data stays on the device during normal use.",
    "where": "landing facts, privacy page, README",
    "test": "npm test -- --grep @claim:project-data-local",
    "sandbox": "browser desktop interface loads sample projects, confirms one, and records no external request"
  },
  {
    "id": "license-token-only",
    "claim": "A license check sends only the pasted license value.",
    "where": "privacy page, README",
    "test": "npm test -- --grep @claim:license-token-only",
    "sandbox": "browser desktop interface intercepts the verification request after a pasted fixture token"
  },
  {
    "id": "checkout-availability",
    "claim": "The site shows a purchase link only when checkout is active and a verified package exists for the visitor's platform.",
    "where": "pricing, terms, README",
    "test": "npm test -- --grep @claim:checkout-availability",
    "sandbox": "mock mismatched and active matching catalogue responses with complete signed and unsigned release responses; assert no checkout without both conditions and the exact product link and price only with both"
  }
]
